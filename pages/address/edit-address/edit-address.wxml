<import src='/components/address-info/address-info.wxml' />

<top-bar/>
<view class='addressList'>
  <block>
    <view catchtap='addressLineClicked'>
      <template is="address-info" data=''/>
      <view class='address-edit-choose flexR-sb whiteBg'>
        <view class='choose-leftbar boxSize {{item.defaultStatus? "active":""}}' bindtap='setDefault' data-index='{{index}}'>
          {{item.defaultStatus? '':"设为"}}默认地址
        </view>
        <view class='choose-rightbar flexR'>
          <view class='edit' bindtap='editAddress' data-index='{{index}}'>编辑</view>
          <view class='del' bindtap='deleteAddress' data-index='{{index}}'>删除</view>
        </view>
      </view>
    </view>
  </block>

  <view class='default-btn fixed-bottom' catchtap='newAddress'>添加新地址</view>
</view>
